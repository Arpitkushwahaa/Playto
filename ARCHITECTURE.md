# Architecture Overview

## System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                         FRONTEND                             │
│                       (React + Tailwind)                     │
│                                                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌────────────┐ │
│  │   Feed   │  │   Post   │  │ Comment  │  │ Leaderboard│ │
│  │Component │  │Component │  │Component │  │  Widget    │ │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └─────┬──────┘ │
│       │             │              │               │        │
│       └─────────────┴──────────────┴───────────────┘        │
│                           │                                  │
│                      ┌────▼─────┐                           │
│                      │ API.js   │                           │
│                      │(Axios)   │                           │
│                      └────┬─────┘                           │
└───────────────────────────┼──────────────────────────────────┘
                            │
                        HTTP/JSON
                            │
┌───────────────────────────▼──────────────────────────────────┐
│                      BACKEND API                             │
│              (Django REST Framework)                         │
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │ PostViewSet  │  │CommentViewSet│  │Leaderboard   │     │
│  │              │  │              │  │ViewSet       │     │
│  │ - list()     │  │ - list()     │  │ - top_users()│     │
│  │ - create()   │  │ - create()   │  │              │     │
│  │ - like()     │  │ - like()     │  │              │     │
│  │ - unlike()   │  │ - unlike()   │  │              │     │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘     │
│         │                  │                  │              │
│         └──────────────────┴──────────────────┘              │
│                           │                                  │
│                   ┌───────▼────────┐                        │
│                   │  Serializers   │                        │
│                   │  - Post        │                        │
│                   │  - Comment     │                        │
│                   │  - Like        │                        │
│                   │  - Leaderboard │                        │
│                   └───────┬────────┘                        │
│                           │                                  │
└───────────────────────────┼──────────────────────────────────┘
                            │
                        ORM Queries
                            │
┌───────────────────────────▼──────────────────────────────────┐
│                       DATABASE                               │
│                    (SQLite/PostgreSQL)                       │
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │ auth_user    │  │  feed_post   │  │ feed_comment │     │
│  │              │  │              │  │              │     │
│  │ - id         │  │ - id         │  │ - id         │     │
│  │ - username   │  │ - author_id  │  │ - post_id    │     │
│  │ - email      │  │ - content    │  │ - author_id  │     │
│  └──────────────┘  │ - like_count │  │ - parent_id  │     │
│                     │ - created_at │  │ - content    │     │
│                     └──────────────┘  │ - tree_path  │     │
│                                       │ - depth      │     │
│  ┌──────────────┐                    │ - like_count │     │
│  │  feed_like   │                    │ - created_at │     │
│  │              │                    └──────────────┘     │
│  │ - id         │                                          │
│  │ - user_id    │   ┌─────────────────────────┐          │
│  │ - post_id    │   │  Database Constraints:  │          │
│  │ - comment_id │   │                         │          │
│  │ - created_at │   │ ✓ Unique(user, post)    │          │
│  └──────────────┘   │ ✓ Unique(user, comment) │          │
│                     │ ✓ Check(post XOR comment)│          │
│                     └─────────────────────────┘          │
└──────────────────────────────────────────────────────────────┘
```

## Data Flow

### Creating a Post

```
User Input → React Feed Component → API.post('/posts/', data)
                                            ↓
                            PostViewSet.create(request)
                                            ↓
                            PostSerializer.validate()
                                            ↓
                            Post.objects.create(...)
                                            ↓
                                    Save to Database
                                            ↓
                            Return PostSerializer(post)
                                            ↓
                            JSON Response → Update UI
```

### Liking a Post (Race Condition Prevention)

```
User Clicks Like → API.post('/posts/1/like/')
                                ↓
                    PostViewSet.like(request, pk)
                                ↓
                transaction.atomic() {
                    Like.objects.get_or_create(
                        user=user, post=post
                    )
                }
                                ↓
                        Database Checks:
                    ┌─────────────────────┐
                    │ Unique Constraint?  │
                    │ Already liked?      │
                    └─────────────────────┘
                        ↓              ↓
                    Success        IntegrityError
                        ↓              ↓
                Return 201      Return 400
                    "Liked"     "Already liked"
```

### Loading Nested Comments (N+1 Prevention)

```
Request Post → PostViewSet.retrieve(pk)
                        ↓
            Post.objects.get(pk=pk)
                .select_related('author')
                .prefetch_related(
                    'comments'  # Level 0
                    .filter(parent__isnull=True)
                    .prefetch_related(
                        'replies'  # Level 1
                        .prefetch_related(
                            'replies'  # Level 2
                            .prefetch_related('replies')  # Level 3
                        )
                    )
                )
                        ↓
            ┌───────────────────────┐
            │ Query 1: Get Post     │
            │ Query 2: Get L0 Comments│
            │ Query 3: Get L1 Replies│
            │ Query 4: Get L2 Replies│
            │ Query 5: Get L3 Replies│
            └───────────────────────┘
                        ↓
            Total: 5 queries (not 50+!)
                        ↓
            CommentSerializer (recursive)
                        ↓
            JSON Response with full tree
```

### Leaderboard Calculation (Last 24h)

```
GET /leaderboard/top_users/
            ↓
LeaderboardViewSet.top_users()
            ↓
twenty_four_hours_ago = now() - 24h
            ↓
User.objects.filter(
    Q(posts__likes__created_at >= 24h_ago) |
    Q(comments__likes__created_at >= 24h_ago)
)
.annotate(
    post_karma = SUM(
        CASE WHEN posts__likes__created_at >= 24h_ago
        THEN 5 ELSE 0
    ),
    comment_karma = SUM(
        CASE WHEN comments__likes__created_at >= 24h_ago
        THEN 1 ELSE 0
    )
)
.annotate(karma = post_karma + comment_karma)
.order_by('-karma')[:5]
            ↓
    SQL Aggregation Query
            ↓
    Return Top 5 Users
            ↓
    JSON Response
```

## Component Hierarchy

```
App.js
├── Header
│   └── Title + Karma Info
│
├── Main
│   ├── Feed (Left Column)
│   │   ├── CreatePostForm
│   │   └── PostList
│   │       └── Post
│   │           ├── PostHeader (Author, Time)
│   │           ├── PostContent
│   │           ├── PostActions (Like, Comment)
│   │           ├── CommentForm
│   │           └── CommentList
│   │               └── Comment (Recursive)
│   │                   ├── CommentHeader
│   │                   ├── CommentContent
│   │                   ├── CommentActions (Like, Reply)
│   │                   ├── ReplyForm
│   │                   └── replies: [Comment, Comment, ...]
│   │
│   └── Leaderboard (Right Column)
│       └── TopUsersList
│           └── UserCard (x5)
│               ├── Medal Emoji
│               ├── Username
│               └── Karma Breakdown
│
└── Footer
    └── Tech Stack Info
```

## Database Schema

```
┌─────────────────┐
│   auth_user     │
├─────────────────┤
│ id (PK)         │◄───────┐
│ username        │        │
│ email           │        │
│ password        │        │
│ ...             │        │
└─────────────────┘        │
                           │
                           │
┌─────────────────┐        │
│   feed_post     │        │
├─────────────────┤        │
│ id (PK)         │        │
│ author_id (FK) ─┼────────┘
│ content         │
│ created_at [IDX]│
│ like_count      │
└────────┬────────┘
         │
         │
         │        ┌─────────────────┐
         │        │  feed_comment   │
         │        ├─────────────────┤
         │        │ id (PK)         │
         └────────┼─ post_id (FK)   │
                  │ author_id (FK) ─┼───┐
                  │ parent_id (FK) ─┼─┐ │
                  │ content         │ │ │
                  │ tree_path [IDX] │ │ │
                  │ depth           │ │ │
                  │ like_count      │ │ │
                  │ created_at [IDX]│ │ │
                  └────────┬────────┘ │ │
                           │          │ │
                           └──────────┘ │
                                        │
                                        │
┌─────────────────┐                    │
│   feed_like     │                    │
├─────────────────┤                    │
│ id (PK)         │                    │
│ user_id (FK) ───┼────────────────────┘
│ post_id (FK)    │
│ comment_id (FK) │
│ created_at [IDX]│
├─────────────────┤
│ UNIQUE(user, post) WHERE post IS NOT NULL
│ UNIQUE(user, comment) WHERE comment IS NOT NULL
│ CHECK(post IS NOT NULL OR comment IS NOT NULL)
│ CHECK(NOT (post IS NOT NULL AND comment IS NOT NULL))
└─────────────────┘
```

## Request Flow Example

```
┌──────────┐
│  Browser │
│ (React)  │
└─────┬────┘
      │
      │ 1. User clicks "Like" on Post #5
      │
      ▼
┌──────────────┐
│ Post.js      │
│ handleLike() │
└─────┬────────┘
      │
      │ 2. Call API: POST /api/posts/5/like/
      │
      ▼
┌────────────┐
│  axios     │
│ (HTTP)     │
└─────┬──────┘
      │
      │ 3. HTTP Request
      │
      ▼
┌──────────────────────┐
│ Django Middleware    │
│ - CORS               │
│ - Authentication     │
└─────┬────────────────┘
      │
      │ 4. Route to ViewSet
      │
      ▼
┌──────────────────────┐
│ PostViewSet          │
│ @action like()       │
└─────┬────────────────┘
      │
      │ 5. transaction.atomic()
      │
      ▼
┌──────────────────────┐
│ Like.objects         │
│ .get_or_create()     │
└─────┬────────────────┘
      │
      │ 6. Database Query
      │
      ▼
┌──────────────────────┐
│ PostgreSQL/SQLite    │
│ - Check constraint   │
│ - Create or reject   │
└─────┬────────────────┘
      │
      │ 7. Result
      │
      ▼
┌──────────────────────┐
│ PostViewSet          │
│ Return Response      │
└─────┬────────────────┘
      │
      │ 8. JSON Response
      │
      ▼
┌──────────────────────┐
│ axios                │
│ .then()              │
└─────┬────────────────┘
      │
      │ 9. Update UI state
      │
      ▼
┌──────────────────────┐
│ Post.js              │
│ setPost({ ...post,   │
│   like_count: + 1 }) │
└─────┬────────────────┘
      │
      │ 10. React re-renders
      │
      ▼
┌──────────────────────┐
│ Browser              │
│ Shows updated count  │
└──────────────────────┘
```

## Technology Stack Diagram

```
┌─────────────────────────────────────────────────────────┐
│                    PRESENTATION LAYER                   │
│  ┌──────────┐  ┌──────────┐  ┌────────┐  ┌──────────┐│
│  │  React   │  │ Tailwind │  │ Axios  │  │   ESLint ││
│  │   18.x   │  │   CSS    │  │        │  │          ││
│  └──────────┘  └──────────┘  └────────┘  └──────────┘│
└─────────────────────────────────────────────────────────┘
                           │
                      REST API
                           │
┌─────────────────────────────────────────────────────────┐
│                     API LAYER                           │
│  ┌──────────┐  ┌──────────┐  ┌────────┐  ┌──────────┐│
│  │  Django  │  │   DRF    │  │ CORS   │  │   JWT    ││
│  │   4.2    │  │   3.14   │  │Headers │  │  (Future)││
│  └──────────┘  └──────────┘  └────────┘  └──────────┘│
└─────────────────────────────────────────────────────────┘
                           │
                        ORM
                           │
┌─────────────────────────────────────────────────────────┐
│                   DATABASE LAYER                        │
│  ┌──────────┐  ┌──────────┐  ┌────────┐  ┌──────────┐│
│  │PostgreSQL│  │  SQLite  │  │Indexes │  │Constraints││
│  │  (Prod)  │  │  (Dev)   │  │        │  │          ││
│  └──────────┘  └──────────┘  └────────┘  └──────────┘│
└─────────────────────────────────────────────────────────┘
                           │
                     Deployment
                           │
┌─────────────────────────────────────────────────────────┐
│                  INFRASTRUCTURE                         │
│  ┌──────────┐  ┌──────────┐  ┌────────┐  ┌──────────┐│
│  │  Docker  │  │ Railway  │  │ Vercel │  │   AWS    ││
│  │ Compose  │  │          │  │        │  │  (Future)││
│  └──────────┘  └──────────┘  └────────┘  └──────────┘│
└─────────────────────────────────────────────────────────┘
```

---

**Built for the Playto Engineering Challenge** 🚀
